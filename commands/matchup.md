---
description: Show current or specific week matchup
allowed-tools: mcp__espn-fantasy-baseball__get_matchup
---

Call `get_matchup`. If the user provided a week number in "$ARGUMENTS", pass it as the week parameter. Otherwise use week=0 for the current week.

Display the category-by-category breakdown directly.

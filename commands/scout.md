---
description: Search free agents by position
allowed-tools: mcp__espn-fantasy-baseball__get_free_agents
---

Call `get_free_agents`. Parse "$ARGUMENTS" for:
- A position filter (e.g., "SS", "SP", "OF") — pass as the position parameter
- An optional count (e.g., "SS 10") — pass as the size parameter, default 25

Display the results directly.
